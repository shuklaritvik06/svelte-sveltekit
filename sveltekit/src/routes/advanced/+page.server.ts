export const trailingSlash = 'always';

/**
 * Server Side Rendering is the concept where
 * full page is built and given to the browser, this is useful
 * where there is no client side interaction involved in the UI
 */
export const ssr = false;

/**
 * Client Side Rendering is the concept where no js is given by the server,
 * the static html is given to the user and then hydration takes place
 * on the client side
 */

// export const csr = false;

/**
 * Prerendering is the concept where the
 * page is build at the building application
 * time and somehow serving static content
 * is much more faster but the
 */

// export const prerender = true;
